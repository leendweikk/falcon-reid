# Inference-only dependencies (Docker). torch + torchvision are installed separately in the
# Dockerfile from the PyTorch CUDA 12.6 index: plain `pip install torch==2.14.0` pulls CUDA 13,
# which needs NVIDIA driver >= 580 and would NOT run on the organizers' driver (CUDA 12.2).
timm==1.0.30
numpy==2.4.6
pandas==3.0.6
pillow==12.3.0
huggingface_hub==1.32.0
safetensors==0.8.0
PyYAML==6.0.3
filelock==3.32.3
fsspec==2026.7.0
packaging==26.3
tqdm==4.70.1
typing_extensions==4.16.0
hf-xet==1.6.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.15.1
idna==3.20
certifi==2026.7.22
click==8.5.0
python-dateutil==2.9.0.post0
six==1.17.0
tzdata==2026.4
